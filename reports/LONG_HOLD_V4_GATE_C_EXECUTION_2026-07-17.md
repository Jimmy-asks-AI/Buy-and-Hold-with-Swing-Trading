# Long Hold V4 Gate C：账户与成交闭环

日期：2026-07-17  
状态：账户与人工成交回执闭环完成；券商连接、分红和公司行为未实现。

## 完成项

- `account.json`迁移为schema V2，核心仓与T仓分账。
- 订单增加确定性ID、信号日、有效期、目标权重、参考股数、核心比例和T持有交易日。
- 新增`pending_fills.csv`、`execution.py`和`fill_ledger.csv`。
- 支持部分成交、真实/模型费用、成本价、已实现盈亏和成交幂等。
- 手工标记不能绕过买入订单；核心仓卖出必须人工批准。
- T买入要求核心仓达到80%，T仓不超过完整目标20%；T卖出执行T+1和最短持有期。
- 单标的上限按核心仓与T仓合计执行；默认单股核心仓完整目标10%，叠加最多2% T仓后不超过12%。
- 已有核心仓在无新入场边际时保持；硬否决只进入人工复核。
- 持仓缺价、离开快照或元数据冲突会阻断新增风险。

## 当前账户迁移结果

```text
schema_version=2
cash_cny=500000.0
holdings=0
processed_fills=0
fill_history=0
```

迁移没有产生交易，也没有改变现金。

## 验证

- V4测试：43/43通过。
- `compileall strategy_lab tests scripts`：通过。
- 当前CLI：`CASH_NO_ENTRY_SIGNAL`。
- 当前订单表为空但保留23个固定字段。
- 空成交台账保留28个固定字段。

## 未完成边界

- 无券商API和自动下单。
- 无撤单状态同步；账户只消费已成交回执。
- 无现金分红、税、送转、拆并股和配股。
- 无日终净值历史及组合回撤刹车。
- ETF仍无真实候选数据。
- 无正式PIT历史walk-forward，不得报告V4预期收益。
